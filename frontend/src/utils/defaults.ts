export const DEFAULT_SORT = "date-dec";

export const DEFAULT_TRANSACTION_DATA = {
  transactions: [],
  total: 0,
  totalItems: 0,
  totalPages: 0,
  currentPage: 1,
};

export const PAGINATION_LIMIT = 10;
